[project]
name = "agent-wireshark"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "langchain-mcp-adapters>=0.2.2",
    "langgraph>=1.1.9",
    "langgraph-supervisor>=0.0.31",
]
